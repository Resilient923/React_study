import React,{ Component } from 'react';
class Subject extends Component { //컴포넌트 만드는 코드
    render(){//render 라고 하는 메소드가 있다. function 생략 class안의 함수는 render
      return (
        <header>
        <h1>WEB</h1>
        world wide web!
    </header>  
      );
    }
  }
  export default Subject;